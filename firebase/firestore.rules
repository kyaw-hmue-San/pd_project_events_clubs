rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the authenticated Express API.
    // Admin SDK access is controlled by IAM and API authorization.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
